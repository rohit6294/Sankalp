const https = require('https');
const crypto = require('crypto');

// Firebase Auth REST API (requires API key)
// I don't have the Web API key easily available, maybe I can just mint a custom token using admin SDK locally.
